int* flatten(int** matrix, int r, int c) {
    int *M= new int[r*c];
    int count=0;
    for (int i=0;i<r;i++)
    {
        for (int j=0;j<c;j++)
        {
            M[count]=matrix[i][j];
            count++;
        }
    }
    return M;
}
