void uppercase(string output)   {
 
    string a;
    cin >>a;
    for (int i=0;i<a.length();i++)
    {
        if (a[i]>=95 && a[i]<=122) a[i]-=32;
    }
    ofstream f(output);
    f << a;
    f.close();
 
 
}
